document.addEventListener("DOMContentLoaded", () => {
  yesNoButtonDiv = document.querySelector(".ui.large.buttons")

  yesNoButtonDiv.addEventListener("click", (e)=>{
    console.log("yeet")
    if(e.target.className === "button"){  
    }
  })

})
